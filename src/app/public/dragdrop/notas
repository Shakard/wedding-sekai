<!-- <div class="guests-container"><br>
  <div class="guest-header">
      <input class="filters" pInputText type="text" name="filters" placeholder="Nombres" [(ngModel)]="names"
        (input)="searchGuest()"/>        
      <input class="filters" pInputText type="text" name="filters" placeholder="Correo" [(ngModel)]="mail"
        (input)="searchGuest()"/>
  </div>
  <div id="invitados" dragula="SPILL" [(dragulaModel)]="users" class="guest-list">
    <div class="guest-box" *ngFor="let user of users">
      {{user.first_name}} {{user.last_name}} {{user.email}} {{user.confirmation}}
    </div>
  </div>
</div> -->